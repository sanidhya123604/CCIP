#include <iostream>

using namespace std;
int fact(int n)
{
    if(n==0)
    {
        return 1;
    }
    else
    {
        return n*fact(n-1);
    }
}

int main()
{
    int n;
    cout<<"enter the value of n:";
    cin>>n;
    int result = fact(n);
    cout<<result;
    
    

    return 0;
}
